import type { StackPanelProps } from "@/components/client-stack/panel-props";

/**
 * Switch the active preset, and fix the technique order when the feed is
 * running before whatever supplies its motion vectors.
 */
export function PresetPanel(_props: StackPanelProps) {
  return null;
}
