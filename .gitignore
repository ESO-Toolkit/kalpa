# Dependencies
node_modules/

# Build output
dist/
target/

# Tauri
src-tauri/target/
src-tauri/gen/
src-tauri/keys
src-tauri/binaries/

# Environment
.env
.env.*
!.env.example
.dev.vars

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Plan file (working doc)
PLAN.md

# Generated types
worker-configuration.d.ts

# Test artifacts
e2e/screenshots/
test-results/
playwright-report/

# Claude Code
.claude/

# DeepSec scanner workspace
.deepsec/

# Throwaway: extracted official-uploader bundle for clean-room format-fact reads
.asar-extract/
.asar-main.js
.cdp-eval.mjs
.asar-*.js

# Large decode-only ESO log samples (not committed; used for rule decoding)
.decode-samples/

# Small native-uploader golden test fixtures ARE committed (they back the
# byte-exact encoder tests) — re-include them past the broad `*.log` rule above.
# Only the small `sample_*`/`chunk1_*` fixtures the tests `include_str!`; the
# multi-MB decode-only fixtures stay untracked (added individually, never by glob).
!src-tauri/src/uploader/native/testdata/*.log
