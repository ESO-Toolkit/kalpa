{
  "name": "eso-packs-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "npm run check && wrangler deploy",
    "check": "npm run check:version-policy && tsc --noEmit && tsc --noEmit -p test/tsconfig.json",
    "check:version-policy": "node scripts/check-version-policy.mjs",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.21.3",
    "@cloudflare/workers-types": "^5.20260714.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.110.0"
  },
  "version": "0.0.0"
}
